Download and install Qt Installer Framework (https://download.qt.io/official_releases/qt-installer-framework/)
On the terminal run the following command to generate the installer
<Folder where you installed the framework>/bin/binarycreator -c config/config.xml -p packages CLOmodelInstaller